<div class="post-hero{{#unless feature_image}} no-img{{/unless}}{{#if featured}} featured{{/if}}">
  <div class="post-hero__content">
    <h1 class="post-hero__title">{{title}}</h1>

    <div style="display: flex; margin-bottom: 30px">
      <div class="post-hero__info">

        <time datetime="{{date format='YYYY-MM-DD'}} {{ date published_at format='h:mm:ss' }}" class="post-hero__date">
          {{date published_at timeago="" format="DD.MM.YYYY"}}
        </time>
      </div>

      <div class="post-hero__header">
        {{#foreach tags to="3"}}
        <a class="tag tag-{{slug}} post-hero__tag" href="{{url}}" title="{{name}}" aria-label="{{name}}s"
          style="margin-bottom: 8px !important;">{{name}}</a>
        {{/foreach}}
      </div>
    </div>


    <div class="post-hero__details">
      {{!-- Post image --}}
      {{#if feature_image}}
      <div class="post-hero__image-wrapper">
        <figure class="post-hero__image">
          <img class="lazyload" data-srcset="{{img_url feature_image size=" s" format="webp" }} 300w, {{img_url
            feature_image size="m" format="webp" }} 600w, {{img_url feature_image size="l" format="webp" }} 1000w,
            {{img_url feature_image size="xl" format="webp" }} 2000w" data-sizes="(max-width: 800px) 50vw,
                    (max-width: 1170px) 60vw,
                    1400px" data-src="{{img_url feature_image size=" xl" format="webp" }}"
            src="{{asset 'images/img-placeholder-md.jpg'}}" alt="{{title}}" />
        </figure>
        {{#if feature_image_caption}}
        <p class="post-hero__image-caption center-xs">{{feature_image_caption}}</p>
        {{/if}}
      </div>
      {{/if}}
    </div>
  </div>
</div>