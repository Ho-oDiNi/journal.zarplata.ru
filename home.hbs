{{!< default}} <div class="container-fluid wrapper">
  <section class="section latest-post mobile">
    <!-- Slider main container -->
    <div class="swiper">
      <!-- Additional required wrapper -->
      <div class="swiper-wrapper">
        <!-- Slides -->
        {{#get "posts" filter="primary_tag:applicants" order="slug asc"}}
        {{#foreach posts to="6"}}
        <div class="swiper-slide">
          <div class="row">
            <div class="col-xs {{#unless feature_image}}m-t-lg m-b-lg{{/unless}}">
              {{> post-card class="post-card--special" lazy=false }}
              <div class="tag" style="position: absolute; top: 12px; left: 12px; z-index: 99">
                <a class="tag-applicants post-card__tag" href="/tag/applicants/" title="{{name}}"
                  aria-label="{{name}}s">Соискателям</a>
              </div>
            </div>
          </div>
        </div>
        {{/foreach}}
        {{/get}}
      </div>
    </div>
  </section>
  <div class="row">
    <div class="col-xs-12 col-lg-12 col-xl-12">
      <section class="section latest-post desktop">
        {{#foreach posts to="1"}}
        <div class="row desktop" style="display: flex;">
          <div class="col-xs {{#unless feature_image}}m-t-lg m-b-lg{{/unless}}" style="margin: 30px 0;">
            {{> post-card class="post-card--main" lazy=false }}
          </div>
          <div class="col-xs-4 col-lg-4 col-xl-4 lg">
            {{> sidebar}}
          </div>
        </div>
        {{/foreach}}

      </section>
      <section class="section all-posts">
        <div class="row posts desktop">
          {{#foreach posts from="2" to="4"}}
          <div class="col-xs-12 col-md-4 post-wrap">
            {{> post-card class="col"}}
          </div>
          {{/foreach}}
        </div>

        <div class="row posts desktop">
          {{#foreach posts from="5"}}
          <div class="col-xs-12 col-md-4 post-wrap">
            {{> post-card class="col"}}
          </div>
          {{/foreach}}
        </div>
        
        <div class="row posts mobile">
          {{#foreach posts from="1" to="3"}}
          <div class="col-xs-12 col-md-6 post-wrap">
            {{> post-card class="col--main"}}
          </div>
          {{/foreach}}
        </div>
        <section class="section latest-post mobile">
          <!-- Slider main container -->
          <div class="swiper" style="padding-bottom: 32px;">
            <!-- Additional required wrapper -->
            <div class="swiper-wrapper">
              <!-- Slides -->
              {{#get "posts" filter="primary_tag:employers" order="slug asc"}}
              {{#foreach posts to="6"}}
              <div class="swiper-slide">
                <div class="row">
                  <div class="col-xs {{#unless feature_image}}m-t-lg m-b-lg{{/unless}}">
                    {{> post-card class="post-card--special" lazy=false }}
                    <div class="tag" style="position: absolute; top: 12px; left: 12px; z-index: 99">
                      <a class="tag-employers post-card__tag" href="/tag/employers/" title="{{name}}"
                        aria-label="{{name}}s">Работодателям</a>
                    </div>
                  </div>
                </div>
              </div>
              {{/foreach}}
              {{/get}}
            </div>
          </div>
        </section>
        <div class="row posts mobile">
          {{#foreach posts from="4" to="6"}}
          <div class="col-xs-12 col-md-6 post-wrap">
            {{> post-card class="col"}}
          </div>
          {{/foreach}}
        </div>
        <div class="row">
          <div class="col-xs-12 center-xs">
            <button id="load-more-posts" class="btn btn--wide btn--primary">
              Показать больше статей
            </button>
          </div>
        </div>
      </section>
    </div>
    <div class="col-xs col-lg-4 col-xl-3 has-sidebar md">
      <div> {{> mobile-sidebar}} </div>
      <div> {{> sidebar}} </div>   
    </div>
  </div>

  </div>